const Character = require('./Character')
const Campaign = require('./Campaign')
const Location = require('./Location')
const User = require('./User')

module.exports = {User, Campaign, Location, Character}